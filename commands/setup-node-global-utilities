#!/usr/bin/env bash

function setup_node_global_packages() (
	source "$DOROTHY/sources/bash.bash"

	# =====================================
	# Configuration

	source "$DOROTHY/sources/config.sh"

	# setup.bash provides:
	local SETUP_GLOBAL_NPM_PACKAGES=()
	load_dorothy_config 'setup.bash'

	# =====================================
	# Arguments

	# help
	function help {
		cat <<-EOF >&2
			ABOUT:
			Setup global Node.js packages via npm.

			USAGE:
			setup-node-global-packages [...options] [install|update|setup]

			OPTIONS:
			--configure
			    Whether to prompt, confirm, or save configuration changes.
		EOF
		if [[ $# -ne 0 ]]; then
			__print_error "$@"
		fi
		return 22 # EINVAL 22 Invalid argument
	}

	# process
	local item option_configure='yes'
	while [[ $# -ne 0 ]]; do
		item="$1"
		shift
		case "$item" in
		'--help' | '-h') help ;;
		# install|update|setup
		'setup') ;;
		'update') option_configure='no' ;;
		'install') option_configure='yes' ;;
		'--no-configure'* | '--configure'*) __flag --source={item} --target={option_configure} --affirmative --coerce || return ;;
		'--'*) help 'An unrecognised flag was provided: ' --variable-value={item} ;;
		*) help 'An unrecognised argument was provided: ' --variable-value={item} ;;
		esac
	done

	# check
	if [[ ${#SETUP_GLOBAL_NPM_PACKAGES[@]} -eq 0 ]]; then
		__print_style --dim='Setup of Node.js global packages skipped (no packages configured).' || return
		return 0
	fi

	# =====================================
	# Start

	__print_style --h1='Setup Node.js Global Packages' || return

	# =====================================
	# Ensure Node.js is available via mise

	__print_style --h2='Ensure Node.js' || return
	if ! command-exists node; then
		if command-exists mise; then
			mise install node
		else
			__print_error 'node not found and mise is not available'
			return 1
		fi
	fi
	__print_style --g2='Ensure Node.js' || return

	# =====================================
	# Install/Update global packages

	if [[ ${#SETUP_GLOBAL_NPM_PACKAGES[@]} -ne 0 ]]; then
		__print_style --h2='Global Packages (npm)' || return

		# Check if any packages are not already installed
		local missing_packages=()
		local installed_packages=()
		if [[ $option_configure != 'yes' ]]; then
			# Get currently installed global packages
			mapfile -t installed_packages < <(npm list -g --depth=0 2>/dev/null | grep -E '^[├└]' | awk '{print $2}' | cut -d@ -f1 || :)

			for pkg in "${SETUP_GLOBAL_NPM_PACKAGES[@]}"; do
				# Extract package name without version (e.g. @google/gemini-cli@1.2.3 -> @google/gemini-cli)
				local pkg_name="${pkg%%@*}"
				if [[ ! " ${installed_packages[*]} " =~ " ${pkg_name} " ]]; then
					missing_packages+=("$pkg")
				fi
			done
		fi

		# Source encrypted security secrets (SOCKET_SECURITY_API_KEY, SNYK_TOKEN, etc.)
		local _sbom_secrets
		_sbom_secrets="$(dirname "${BASH_SOURCE[0]}")/../config.local/sbom-security.sh"
		if [[ -f "$_sbom_secrets" ]]; then
			# shellcheck source=/dev/null
			source "$_sbom_secrets"
		fi

		# Use socket npm wrapper when available for supply chain protection.
		local npm_cmd='npm'
		if command -v socket >/dev/null 2>&1; then
			if [[ -n ${SOCKET_SECURITY_API_KEY:-} ]]; then
				npm_cmd='socket npm'
			else
				__print_style --dim='socket installed but SOCKET_SECURITY_API_KEY not set — using plain npm.' || :
			fi
		else
			__print_style --dim='socket not installed — using plain npm. Run setup-util-security-scanners to enable supply chain scanning.' || :
		fi

		# Only install if configure=yes or there are missing packages
		if [[ $option_configure == 'yes' ]] || [[ ${#missing_packages[@]} -ne 0 ]]; then
			if [[ $option_configure == 'yes' ]]; then
				# Install all configured packages
				$npm_cmd install -g "${SETUP_GLOBAL_NPM_PACKAGES[@]}"
			else
				# Install only missing packages
				if [[ ${#missing_packages[@]} -ne 0 ]]; then
					$npm_cmd install -g "${missing_packages[@]}"
				fi
			fi
		fi

		# Update existing packages
		if [[ $option_configure == 'no' ]]; then
			$npm_cmd update -g "${SETUP_GLOBAL_NPM_PACKAGES[@]}"
		fi

		__print_style --g2='Global Packages (npm)' || return
	fi

	# =====================================
	# Finish

	__print_style --g1='Setup Node.js Global Packages' || return
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_node_global_packages "$@"

	echo "Installing claude code"
	curl -fsSL https://claude.ai/install.sh | bash
fi
